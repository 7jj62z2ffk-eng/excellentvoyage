# 嘉旅国际物流官网 · 内容更新维护指南

适用对象：网站运营 / 内容维护人员（非专业开发也可照做）
最后更新：2026-07-27 ｜ 维护人：罗龙明（Davis）

---

## 1. 站点基本情况

| 项目 | 说明 |
|------|------|
| 站点主体 | 深圳市嘉旅国际物流有限公司 / Excellent Voyage Logistics |
| 技术形态 | **纯静态 HTML 展示站**，无后台 CMS，无数据库 |
| 源码仓库 | `https://github.com/7jj62z2ffk-eng/excellentvoyage.git` |
| 发布方式 | GitHub Pages，**站点根目录为仓库内的 `/docs`** |
| 生效时间 | 推送到仓库后 **约 1 分钟**自动重新发布，浏览器强刷（Ctrl+F5）可见 |
| 正式域名 | https://www.excellent-voyage.cn |

> 关键点：你改的是 `/docs` 下的 HTML/JS 文件，推送后 GitHub 自动生成网页，**不需要手动上传 FTP**。

---

## 2. 文件地图（只列常改的）

| 文件 | 作用 | 改什么 |
|------|------|--------|
| `docs/index.html` | 首页 | 横幅文案、核心数据 |
| `docs/about.html` | 关于我们 | 公司介绍、发展时间线 |
| `docs/services.html` | 服务项目 | 业务板块、运价/服务说明 |
| `docs/news.html` | 新闻资讯 | 行业动态、公司新闻（详见 §4.1） |
| `docs/contact.html` | 联系我们 | 电话、邮箱、地址、WhatsApp |
| `docs/js/i18n.js` | **双语词典** | 所有中文/英文文案（详见 §3） |
| `js/i18n.js` | 开发源（与上面内容一致） | 同步修改，保持两份一致 |

> ⚠️ `docs/js/i18n.js` 与 `js/i18n.js` 是**同一份内容的两份副本**，改文案时必须**两处都改**，否则中英文会不一致。

---

## 3. 双语更新规则（最重要）

全站文案集中在 `docs/js/i18n.js`（和 `js/i18n.js`），结构如下：

```js
const i18n = {
  zh: {                       // 中文文案
    about_banner_desc: '深圳市嘉旅国际物流有限公司成立于2022年…',
    about_timeline_2022_title: '公司成立',
    // …… 所有 key 都在这里
  },
  en: {                       // 英文文案（key 名与 zh 完全一致）
    about_banner_desc: 'Excellent Voyage Logistics was established…',
    about_timeline_2022_title: 'Company Founded',
  }
};
```

页面 HTML 通过 `data-i18n="key名"` 关联文案，例如：

```html
<h3 data-i18n="about_timeline_2022_title">公司成立</h3>
```

**规则：**
1. 改文案 → 改 `i18n.js` 里对应 key 的**值**（引号内文字），HTML 里的 `<h3>…</h3>` 只是兜底显示，会被脚本覆盖。
2. **中文改了，英文必须同步改**，否则切到英文页面会缺翻译（显示中文兜底或空白）。
3. **不要随便改 key 名**（如 `about_timeline_2022_title`），改了要同步改 HTML 里的 `data-i18n` 和两份 i18n 文件，容易漏。

---

## 4. 常见更新场景

### 4.1 更新新闻资讯（docs/news.html）

新闻用 **"卡片 + 弹窗"** 机制：列表页只显示摘要，点击"查看详情"弹出全文。

- **改某篇新闻的标题/摘要**：直接改 `docs/news.html` 里对应 `<article>` 的 `<h3>` 和 `<p class="news-excerpt">`。
- **改某篇新闻的全文**：改文件末尾 `<script>` 里的 `newsData` 对象（n1–n6 各一篇）：

```js
const newsData = {
  n1: { date: '2025-11-19', badge: '公司新闻', title: '嘉旅国际物流获无船承运人（NVOCC）资质备案',
        body: '<p>全文第一段…</p><p>全文第二段…</p>' },
  // n2 ~ n6 同理
};
```

- **新增一篇新闻**：① 在新闻列表加一个 `<article>` 卡片（复制现有卡片，改 `data-id="n7"`、`onclick="openNews('n7')"` 和摘要）；② 在 `newsData` 里加 `n7: { date, badge, title, body }`。中英文切换不影响新闻正文（新闻正文目前仅中文）。

> 真实新闻务必来自可查的公开来源（航运界网、Drewry、MarineLink、IMO 官网等），**不要编造数据**。

### 4.2 更新公司介绍 / 发展时间线（docs/about.html + i18n）

- 公司介绍段落：改 `i18n.js` 的 `about_company_intro1` 等 key（中英同步）。
- 发展时间线：当前 4 个节点 **2022 公司成立 → 2023 服务网络升级 → 2025 NVOCC 资质备案 → 2026 持续发展**。
  - 改某年描述：改 `i18n.js` 对应 `about_timeline_202X_desc`（中英同步）。
  - 加/删节点：改 `docs/about.html` 的 `<div class="timeline-item">` 块，**同时**在 `i18n.js` 加/删对应 key。
  - ⚠️ 时间线年份**不得早于成立年 2022**，否则逻辑矛盾。

### 4.3 更新联系方式（docs/contact.html + i18n）

- 电话 / 邮箱 / 微信 / WhatsApp / 地址：改 `docs/contact.html` 对应区块；若文案在 i18n（如 `contact_info_phone_value`），**同步改 i18n 两份**。
- 当前主号：**+86 13590173320**（罗龙明 Davis）；WhatsApp 同号；微信 davis003。

### 4.4 更新服务 / 运价（docs/services.html）

- 业务板块文字：改 `i18n.js` 对应 key。
- 运价数字属于业务敏感信息，更新前务必核对船司最新报价，结构化分栏（港点 / 运费 / 附加费 / 时效）呈现。

---

## 5. 发布流程（提交并推送）

改完文件后，用 Git 提交并推送，GitHub Pages 会自动重发：

```bash
# 1) 查看改了哪些文件
git status

# 2) 把改动加入暂存区（全部提交用 git add -A；只提交某文件写具体路径）
git add -A

# 3) 写提交说明（用中文简述改了什么）
git commit -m "更新：2026年8月运价 + 新增红海绕航新闻"

# 4) 推送到远端（首次可能要求登录/输令牌）
git push origin main
```

- 推送成功后约 1 分钟，访问 https://www.excellent-voyage.cn 强刷即可见。
- 若 `git push` 报网络错误（如 `Could not connect to server` / `Connection reset`），**多试几次**即可；仍不通时检查本机网络/代理软件。
- 令牌：使用**经典令牌（classic token，勾选 repo 权限）**，不要用细粒度令牌（会 403）。用后建议到 GitHub Tokens 页面 revoke。

---

## 6. 避坑清单

1. **i18n 必须中英同步**：只改中文不改英文，英文页会缺翻译。
2. **两份 i18n 文件同步**：`docs/js/i18n.js` 与 `js/i18n.js` 内容要保持一致。
3. **保留原换行风格**：用代码编辑器直接改，不要整文件复制粘贴（会把换行从 CRLF 变 LF，产生整文件 diff 噪声，虽不影响功能）。
4. **不引入死代码**：删功能时要 HTML + JS + CSS 一起删干净；只删 HTML 容器、留着 JS/CSS，会导致控制台报错（本站在 2026-07 已清理过一次分页/自动聚合死代码）。
5. **资质/备案真实性**：NVOCC 编号 GD20251119163617、成立 2022-09-22、注册资金 200 万、4 地分支等均为真实信息，更新时保持真实，切勿虚构。
6. **先本地预览再推送**：不确定效果时，可用浏览器直接打开本地 `docs/xxx.html` 预览，无误后再 `git push`。

---

## 7. 回滚（改错了怎么办）

```bash
# 查看历史提交
git log --oneline

# 撤销某次提交（保留改动到工作区，可再改）
git revert <提交号>

# 或回退到某个历史版本（谨慎，会丢弃之后的提交）
git reset --hard <提交号>
```

不确定时，**先 `git status` 确认没有未保存改动**，再找最近一次正常版本 revert。

---

维护人签收：__________________ 　日期：______________
